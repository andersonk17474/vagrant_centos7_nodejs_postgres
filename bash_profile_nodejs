# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs
NODE_PATH=$HOME/local/node/lib/node_modules
export NODE_PATH

PATH=$HOME/local/node/bin:$HOME/.local/bin:$HOME/bin:$PATH

export PATH

